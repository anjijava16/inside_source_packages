# inside_source_packages
Understanding the source packages 


# IMP packages
1. pip install azure-ai-inference
2. pip install databricks-agents (%pip install -U -qqqq databricks-agents>=0.16.0 mlflow>=2.20.2)
